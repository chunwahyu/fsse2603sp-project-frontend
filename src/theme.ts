import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    background: {
      default: '#F0EEE9',
    },

    text: {
      primary: '#212529',
      secondary: '#36454F',
    },
  },
});

export default theme;